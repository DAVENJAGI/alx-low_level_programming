#include "main.h"
#include <stdio.h>

/**
 * mul -multiplies two integers
 * @a: Integer being multiplied
 * @b: Integers being multiplied
 * Return: Always 0
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
